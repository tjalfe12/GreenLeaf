//A page for allowing the user to edit their own profile. However we decided not to include it, as we didn't have time to implement it properly.

export default function Userpage() {
  return <h1>Userpage</h1>;
}
